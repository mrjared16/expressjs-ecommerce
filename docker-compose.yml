version: '3'
services:
  mongodb:
    image: mongo
    ports:
      - "27017:27017"

  mongo-seed:
    build: ./database-seed
    links:
      - mongodb

  app:
    container_name: docker-ecommerce
    restart: always
    build: .
    ports:
      - '80:3000'
    links:
      - mongodb